name: WordPress Plugin CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.2"
          coverage: none

      - name: Validate PHP syntax
        run: find . -name "*.php" -not -path "./vendor/*" -print0 | xargs -0 -n1 php -l

      - name: Check required plugin files
        run: |
          test -f adminx-productivity.php
          test -f readme.txt

      - name: Build plugin zip
        run: |
          mkdir -p build/adminx-productivity
          rsync -a \
            --exclude='.git*' \
            --exclude='.github' \
            --exclude='build' \
            --exclude='docs' \
            --exclude='node_modules' \
            --exclude='vendor' \
            ./ build/adminx-productivity/
          cd build
          zip -r adminx-productivity.zip adminx-productivity/

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: adminx-productivity
          path: build/adminx-productivity.zip
